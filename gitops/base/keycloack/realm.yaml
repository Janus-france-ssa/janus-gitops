kind: KeycloakRealm
apiVersion: keycloak.org/v1alpha1
metadata:
  name: keycloakrealm
  namespace: sso
spec:
  realm:
    id: basic
    realm: basic
    enabled: true
    displayName: Basic Realm
  instanceSelector:
    matchLabels:
      app: sso

